import { LinearProgress } from "@mui/material";

const Home: React.FC = () => {
  return (
    <div className="flex-1">
      <LinearProgress sx={{ maxWidth: "360px" }} />
    </div>
  );
};

export default Home;
